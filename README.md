# moscat

Just a TCP/UDP forwarder.

```text
-d string
    destination addr
-l string
    listen addr
-t duration
    timeout (default 5m0s)
-u    forward udp
-U    only forward udp (disable tcp forwarding)
```